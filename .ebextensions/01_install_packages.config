packages:
    yum:
        amazon-linux-extras: ""  # Proper indentation and empty string instead of array
commands:
    01_postgres_activate:
        command: sudo amazon-linux-extras enable postgresql15
    02_postgres_install:
        command: sudo yum install -y postgresql-devel